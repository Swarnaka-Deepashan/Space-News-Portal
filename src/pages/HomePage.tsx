import { Box } from "@mui/material";
import TopStoryCard from "../components/TopStoryCard";

const HomePage = () => {
  return (
    <Box>
      <TopStoryCard />
    </Box>
  );
};

export default HomePage;
